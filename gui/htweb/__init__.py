"""Web frontend for the HT_MK1 operator GUI.

``index.html`` carries the markup and CSS of ``Doc/HT_MK1_GUI_Proposal.html``
unchanged, so the running instrument looks exactly like the approved design.
``server.py`` bridges that page to ``htproto``.

    python -m htweb --port 46000
"""
