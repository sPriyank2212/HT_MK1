================================================================================
 HT_MK1  -  AUTOMATED HARNESS TEST SYSTEM  -  FIRMWARE STATUS
 MCU: STM32G474RET6 (Cortex-M4F, FreeRTOS, STM32CubeIDE / HAL)
 Last updated: 2026-08-12 (DOC-01 sync)
================================================================================
 LIVE TRACKING IS PROJECT_LOG.md, NOT THIS FILE. This file is a snapshot of
 the architecture and driver status for quick orientation; open items, dates
 and decisions are tracked in PROJECT_LOG.md and may have moved on since the
 "Last updated" line above. If the two disagree, PROJECT_LOG.md wins.
================================================================================
 SUPERSEDED SINCE THE ABOVE SYNC (2026-08-12, same day): DAC8775 is GONE from
 the schematic. Every DAC8775 reference below (current-source register map,
 SPI2 8-bit framing, control_frontend.c's IDAC half) describes hardware that
 no longer exists. The Kelvin excitation current now comes from the
 ADS124S08's own internal IDAC (routed to HI_COM via AIN9), capped at 2 mA -
 not the DAC8775's supposedly-open range. Firmware has NOT been updated for
 this yet - see Doc/idac_current_source.md for the full account and
 PROJECT_LOG.md for the open item. Do not trust this file's DAC8775 sections
 until that lands.
================================================================================

SOURCE OF TRUTH
--------------------------------------------------------------------------------
The Doc/ folder is now authoritative:
  - Control_Card-5.pdf, Matrix_Card-7.pdf, HV_Card-3.pdf   (finalised KiCad,
    current as of this update; supersede all earlier-numbered PDFs, including
    the -1/-2/-3/-4/-6 series referenced by older revisions of this file)
  - Harness_Tester_Operation_Document.docx  (v1.1 original)
  - Harness_Tester_Operation_Document_v1.3.docx  (was v1.2_corrected; renamed
    and updated to v1.3 - firmware review, see the v1.3 section below)
  - Operation.txt  (short operation notes)
The old root-level schematic PDFs were removed. Where the Operation Document
prose disagreed with the schematics, the SCHEMATICS WIN; the corrected doc was
carried v1.1 -> v1.2 -> v1.3 (change lists at the bottom of this file).

ARCHITECTURE (key fact)
--------------------------------------------------------------------------------
Single-MCU design. The STM32G474 on the Control Card is the ONLY processor in
the instrument. The Matrix Card and HV Cards have NO MCU - they are I2C GPIO
expanders (MCP23017), analogue muxes (CD4067), reed relays (MHV05), DACs and
ADCs driven by this one firmware over I2C / SPI / GPIO. All test logic lives here.

    Core/Inc/drivers, Core/Src/drivers   - device drivers
    Core/Inc/cards,   Core/Src/cards     - per-card abstraction
    Core/Inc/test,    Core/Src/test      - continuity / kelvin / insulation
    Core/Inc/bsp,     Core/Src/bsp       - board bring-up (bindings)
    Core/Inc/app,     Core/Src/app       - logging + RTOS tasks

BUS / PERIPHERAL MAP  (CURRENT as of FW-02 / CL-23, 2026-08-11)
--------------------------------------------------------------------------------
    I2C2  PC4(SCL)/PA8(SDA)   -> SHARED isolated bus (ADUM1250): all four HV
                                 cards' MCP23017s AND the Matrix Card's own
                                 eight expanders (hi_en/lo_en/hi_sns/lo_sns,
                                 U101/102/105/106/66/67/107/108) AND U69 (the
                                 ADS124S08's I2C-expander io lines) all live
                                 here at the same hard-strapped 0x20-0x27
                                 range. Firmware gates it one card/segment at a
                                 time via HV_Card_EN1..4 (Matrix Card = EN1) -
                                 see Doc/i2c_bus_sharing.md.
    I2C3  PC8(SCL)/PC9(SDA)   -> LOCAL to the Control Card, never shared: U20/
                                 U21 (mux address lines HI_S1-4/LO_S1-4).
    SPI1  PA5/6/7, soft-CS via I2C (U69) -> ADS124S08 U68 on the Matrix Card,
                                 reading HI_SENSE/LO_SENSE = RESISTANCE measure
                                 (4-wire Kelvin, 24-bit delta-sigma, PGA auto-
                                 ranged). Matrix U33 (AD7476, HI_COM) was
                                 REMOVED from the schematic in the Matrix rev
                                 that added the Kelvin sense array - it no
                                 longer exists on the card at all.
                                 8-bit, SPI mode 1, <=10 MHz (the part's
                                 ceiling; CubeMX's leftover 4-bit/mode-0/32 MHz
                                 default from the deleted U33 was wrong for
                                 this part and has been fixed in spi.c/.ioc).
    SPI2  PB13/14/15, CS PB2  -> DAC8775 + HV DAC8830 + HV AD7476 x2 (ISOLATED,
                                 shared bus, per-device CS)
    SPI3  PB3/4/5, CS PB1     -> Control AD7476 U4 (ADC_IN) = CONTINUITY measure
    GPIO  OPT0_CNTR = PC12 (mode switch; the Op-Doc's "PA15" was WRONG = buzzer)
          HV_Card_x.0 / x.1  = the two ADC chip-selects per HV board (see below)
          HV_Card_EN1..4     = per-slot I2C2 bus-segment gate (PC5/PC6/PA10/
                                 PA9); EN1 is the Matrix Card's own slot (J1)

    - DAC8830 (HV V_PGM) is on SPI, NOT I2C (Op-Doc said I2C - wrong).
    - There is NO HV-enable and NO discharge GPIO: CA05P-5 VIN is tied to
      +5V_ISO, HV output follows the DAC only. HV OFF = program DAC to 0.
      Discharge is passive through the ~10.05 Mohm sense divider.
    - Each HV board has TWO AD7476: RAIL (U301 = HV_Sense) and LEAK (U302 =
      HV_RET, across R3004 1k = insulation). HV-card ADCs run off +5V_ISO ->
      vref 5.0 V. Control/Matrix ADCs = 3.3 V.
    - HV_Sense = HV * R3003/(R3001+R3003) = HV * 0.00049 ~= 0.245 V @500 V, a
      1:2000 divider (R3001 10M : R3003 5k). CONFIRMED (HW-08, 2026-08-12):
      HV_Card-3.pdf now shows R3003 = 5k, matching this scaling - the earlier
      50k (1:200 -> 2.49 V, x10) drawing error is resolved. Firmware's 0.245 V
      / 0.00049 scaling was already correct against the intended value.

PER-BUS SPI SETTINGS NEEDED (change in CubeMX - generated default is 4-bit!)
--------------------------------------------------------------------------------
    SPI1 (Matrix AD7476)         : 16-bit, RX, SCLK <= 20 MHz, soft-CS PB0.
    SPI2 (DAC8775/DAC8830/AD7476): MIXED - DAC8775 = 8-bit (3-byte frame),
                                   DAC8830/AD7476 = 16-bit. Set data size per
                                   transaction, or run 8-bit and byte-frame.
                                   <= 20 MHz (AD7476 limits the bus).
    SPI3 (Control AD7476)        : 16-bit, RX, SCLK <= 20 MHz, soft-CS PB1.


================================================================================
 COMPLETED
================================================================================

1) MCP23017 driver  -  Core/{Inc,Src}/drivers/mcp23017.{h,c}
--------------------------------------------------------------------------------
   Bus-agnostic 16-bit I2C GPIO expander driver (2x Matrix, 8x per HV Card).
   Init (HAEN, latch low, all outputs), WriteReg/ReadReg(16), WritePins/ReadPins,
   WritePin via shadow latch. Blocking HAL, 100 ms timeout. Complete.

2) Matrix Card layer  -  Core/{Inc,Src}/cards/matrix_card.{h,c}
--------------------------------------------------------------------------------
   SUPERSEDED by the geometry described under "MATRIX CARD REV 2" and the
   4-wire Kelvin rework below (32 muxes/bank, 8:1 CD74HC4051, sense array,
   ADS124S08) - kept here only as the original 2026-07-14 baseline text.
   Routes any of 256 pins onto HI/LO bus: shared 4-bit select per bank
   (HI_S0..3 / LO_S0..3), one-hot enables (HI_EN1..16 U101=0x20, LO_EN1..16
   U102=0x21), break-before-make. MATRIX_EN_ACTIVE_LOW=1 (CD4067 E active-low).
   NEW 2026-07-14: on-card AD7476 (U33 on SPI1, reads HI_COM) added -
   MatrixCard_InitAdc / ReadRaw / ReadVolts. This is the RESISTANCE sense point.
   NOTE: S0..3 pulled down + LO_COM pull-down = 100R (page 3) - the 100R also acts
   as the current-source return and the continuity low leg. Channel address is
   defined at power-up; the mux ENABLE lines are not pulled and float until the
   MCP23017s init (low risk; EN pull-ups to be added next schematic rev).

3) AD7476 driver  -  Core/{Inc,Src}/drivers/ad7476.{h,c}
--------------------------------------------------------------------------------
   SPI read-only 12-bit SAR driver, reused three places today: Control U4
   (SPI3, continuity), HV U301/U302 (SPI2-iso, rail/leak). Matrix U33 (SPI1)
   was the fourth use as of 2026-07-14; U33 was removed from the schematic
   in the Matrix Kelvin rework and SPI1 now carries the ADS124S08 instead
   (see the BUS/PERIPHERAL MAP above). Init/ReadRaw/CodeToVolts/ReadVolts.

4) Schematic reconciliation (Doc/ folder)  -  2026-07-14
--------------------------------------------------------------------------------
   Full read of all three Doc/ schematics + Operation Document. Corrected the
   firmware bus/ADC map and produced Doc/..._v1.2_corrected.docx (change list
   at bottom). Confirmed: DAC8830=SPI; OPTO_CNTR=PC12; HV_Card_x.0/x.1 = the two
   ADC chip-selects (not HV enable/discharge); matrix I2C isolator ADUM1205ARZ
   is BIDIRECTIONAL (earlier "write-only" worry RESOLVED).

5) Driver/card/test stack  (all compile clean, arm-none-eabi-gcc, real HAL)
--------------------------------------------------------------------------------
   Drivers:  dac8830.{h,c} (16-bit V-DAC), dac8775.{h,c} (quad IDAC; REGISTER
             MAP constants still to VERIFY vs datasheet).
   Cards:    control_frontend.{h,c} - DAC8775 + Control AD7476 (SPI3) + OPT0_CNTR.
             hv_card.{h,c} - REWORKED 2026-07-14: 8x MCP23017 (break-before-make),
             DAC8830, TWO AD7476 (adc_rail=HV_Sense, adc_leak=HV_RET), HvCard_HvOff
             / HvEnable(0)=DAC0 / Discharge=passive no-op, the two per-card GPIO
             are the ADC chip-selects. Relay drivers active-high (MCP all-low=safe).
   Tests:    continuity.{h,c} - reads Control U4 (SPI3); connected band 1.3-1.7 V.
             kelvin.{h,c}     - SUPERSEDED 2026-08-11 (FW-02, CL-23): no longer
                                 reads Matrix U33 (removed from the schematic);
                                 reads the ADS124S08 (HI_SENSE/LO_SENSE, 4-wire)
                                 instead - see the PENDING-FIRMWARE entry above.
             insulation.{h,c} - full safe HV sequence; reads HV_RET leakage node;
             GO/NO-GO verdict (PASS < 0.045 V, FAIL >= 0.045 V = 10 Mohm limit).
   BSP:      board.c - Board_Init instantiates g_matrix (+ADC), g_frontend, g_hv[]
             to a safe idle state. SPI map corrected; open bindings marked VERIFY.

6) Logging + RTOS tasks  -  Core/{Inc,Src}/app/{log,tasks}.{h,c}
--------------------------------------------------------------------------------
   Leveled non-blocking logger (drop-if-full queue -> LPUART1 PA2/PA3 = Nucleo
   VCP). Tasks: tSafety(High, forces safe-all on fault), tSequencer(Nrm, one cmd
   under HW mutex), tComms(BelowN, c/k/i/s/f/r VCP console), tLogger(Low).
   Board_Init in main.c USER CODE 2; Tasks_Init in app_freertos RTOS_THREADS.


================================================================================
 FIRMWARE FIXES APPLIED  (2026-07-14, all build clean)
================================================================================
   [x] ADC vref 3.0 -> 3.3 V; added BOARD_VREF_HV = 5.0 V (HV ADCs off +5V_ISO)
   [x] Continuity: connected band 1.3-1.7 V, open >= 3.0 V (was <= 0.5 V bug)
   [x] Kelvin InAmp gain 10 -> 1 (no InAmp in the path)
   [x] board.c SPI map: control ADC -> SPI3, HV bus -> SPI2, matrix ADC -> SPI1
   [x] HvCard reworked: two ADCs, DAC-based HV on/off, GPIO = ADC chip-selects
   [x] SUPERSEDED (FW-02, CL-23, 2026-08-11): resistance now reads the
       ADS124S08 (HI_SENSE/LO_SENSE, 4-wire Kelvin) instead of Matrix U33 -
       see the PENDING-FIRMWARE resistance-test entry below. Continuity still
       reads Control U4, unchanged.
   [x] Insulation OPEN #4 RESOLVED: go/no-go by HV_RET voltage (0.045 V = 10 Mohm)


================================================================================
 PENDING  -  FIRMWARE
================================================================================
   [!] insulation return scheme (v1.3, per HW owner): the leakage loop is
       +500V -> R3002 1M -> HS relay[n] -> HS wire -> [insul] -> adjacent conductor
       -> its LS relay -> HV_RET -> R3004 1k -> GND. Per line: energise HS[n] and
       close ALL Low-Side relays EXCEPT net n's own return (from the connection
       map); the net is measured against every other conductor. insulation.c MUST
       drive this LS pattern - with no LS relay closed the ADC reads ~0 = false PASS.
   [x] SUPERSEDED - the 2-wire resistance test described here (U33 vs GND,
       100R LO_COM return, 10 mA target, mux-Ron self-cal) no longer applies.
       The Matrix Card is 4-wire Kelvin now: a second "sense" mux array taps
       HI_SENSE/LO_SENSE directly at the harness pins (no mux Ron in the
       reading), read by an ADS124S08 (24-bit delta-sigma, PGA auto-ranged
       1-128) instead of U33. `test/kelvin.c` (FW-02, CL-23, 2026-08-11):
       forces excitation, auto-ranges the PGA (highest gain first, stepping
       down on saturation), takes a second same-gain reading with excitation
       off as a per-point zero-current baseline, subtracts it, then converts
       with ADS124S08_OhmsFromCurrent (not the ratiometric form - HW-04, which
       would let R131 serve as a current reference, is not done). Excitation
       current is KELVIN_FORCE_CURRENT_A in kelvin.h - still a TUNE/VERIFY
       placeholder (0.010f in code) pending BU-01 on real hardware; the
       validated *target* is 3 mA (usable window ~1-8 mA), not 10 mA - see
       Doc/4wire_resistance_validation.md. Current reversal for thermal EMF
       (BU-10) is deliberately NOT implemented yet - it needs the DAC8775
       bipolar range, whose register map is still VERIFY (dac8775.h).
   [x] continuity level (v1.3): LO_COM pull-down = 100R. Connected node = divider of
       ~10k pull-up (3.3 V) vs (2*mux_Ron + 100R) -> WELL below the old 1.5 V (a few
       hundred mV, or tens of mV with low-Ron mux); open ~3.3 V. Large margin - set
       thresholds from a bring-up loopback, not a fixed 1.5 V.
   [x] SUPERSEDED - host command layer: `app/proto.c` (FW-05, CL-11,
       2026-08-01) replaced the single-char VCP console with the full line-
       based GUI protocol (Doc/GUI_development_brief.md), including
       structured `!` event streaming during a run.
   [ ] full-scan orchestration (walk all pin pairs vs a wiring schedule) +
       board-to-pin map for multi-HV-board coverage (needs 4 HV cards for 256)
   [x] SUPERSEDED - per-card I2C bus-select: implemented as HV_Card_EN1..4
       (FW/CL-24, CL-25, 2026-08-11), gating one card's segment of the shared
       I2C2 bus at a time - EN1 also covers the Matrix Card's own expanders,
       confirmed on the same shared bus. See Doc/i2c_bus_sharing.md.
   [ ] stepped HV ramp verification (10%/step, per-step HV_Sense check @0.00049
       => ~0.245 V @500 V; confirm the divider is 1:2000 - see HV_Sense note above)
   [ ] enable IWDG in CubeMX + refresh from tSafety (hook already in place)
   [ ] dac8775 register-map constants: VERIFY vs datasheet (current-source mode)

CONFIG TODO (CubeMX / .ioc - REQUIRED before the code runs on hardware)
--------------------------------------------------------------------------------
   [ ] SPI1 -> 16-bit RX, <= 20 MHz, soft-CS PB0 (assign PB0 as GPIO output)
   [ ] SPI2 -> mixed 8/16-bit handling (DAC8775 8-bit, DAC8830/AD7476 16-bit)
   [ ] SPI3 -> 16-bit RX, <= 20 MHz, soft-CS PB1
   [ ] OPT0_CNTR (PC12), HV ADC-CS lines (HV_Card_x.0/x.1) : INPUT -> OUTPUT
   [ ] HV DAC8830 CS (CS_ISO) pin: confirm from connector netlist (placeholder PC2)


================================================================================
 PENDING  -  HARDWARE / CONFIG  (owner decisions, 2026-07-14)
================================================================================
   [x] HV inhibit NOT required - DAC->opamp drives CA05P VPGM; HV off = DAC 0,
       passive bleed via 10.05 Mohm. (FW must size the discharge wait to the
       harness+output capacitance; INSULATION_DISCHARGE_MS = 200 ms today -
       confirm CA05P output capacitance.)
   [x] Reed relays are COLD-switched (discharge before switching) to protect the
       MHV05 contacts from arc erosion. FW sequence already does this.
   [x] Insulation = go/no-go only, 10 Mohm <-> 0.045 V (no leakage grading).
   [x] AD7476 bus - RESOLVED (SPI1 Matrix / SPI2 HV / SPI3 Control).
   [x] Matrix/HV I2C isolators bidirectional (ADUM1205 / ADUM1250) - reads OK.
   [ ] Control-Card <-> Matrix connector: HI_COM/LO_COM + HI_S0..3/LO_S0..3 (8
       MCU GPIO) + isolated I2C + power. Until drawn, matrix select GPIOs are NULL.
   [x] SUPERSEDED - per-card differentiation: solved in firmware via
       HV_Card_EN1..4 bus-segment gating (CL-24/CL-25) rather than a
       per-card ISO1251 + chip-select scheme; see the BUS/PERIPHERAL MAP.
   [ ] EN-line pull-ups on the matrix CD4067 (next schematic rev).
   [ ] DAC8830 net naming: rename I2C-style nets to SPI in next schematic rev.


================================================================================
 OPERATION DOCUMENT v1.2 - CORRECTIONS APPLIED
================================================================================
   File: Doc/Harness_Tester_Operation_Document_v1.2_corrected.docx (40 edits)
   - OPTO_CNTR pin PA15 -> PC12 (PA15 is the buzzer).
   - DAC8830 bus I2C -> SPI everywhere (write-only; no register readback).
   - HV_Sense = rail monitor: 500 V -> ~0.245 V (ratio 0.00049); ramp table and
     all "~0.045 V" rail confirmations rescaled; HV ADC vref 3.3 -> 5.0 V.
   - HV_RET = leakage go/no-go: 0.045 V = 10 Mohm boundary (PASS below / FAIL at).
   - ADC/bus map: SPI1 Matrix (HI_COM, resistance), SPI2 HV x2 (isolated),
     SPI3 Control (ADC_IN, continuity). Resistance measured at HI_COM, not ADC_IN.
   NOTE: the graded insulation-band tables (S1.3, S4.2.3) were left in place with
   an added go/no-go note; delete the graded rows manually if a pure PASS/FAIL
   table is preferred.


================================================================================
 OPERATION DOCUMENT v1.3 - FIRMWARE REVIEW (2026-07-17)
================================================================================
   File: Doc/Harness_Tester_Operation_Document_v1.3.docx (renamed from
   _v1.2_corrected). Full review of the v1.3 doc against the re-issued
   Control_Card-1 / Matrix_Card-3 / HV_Card-1 schematics. Doc now carries inline
   "FIRMWARE REVIEW NOTE (v1.3)" call-outs + an "Open Hardware Decisions" block.
   Findings + HW-owner resolutions (2026-07-17):
   - HV_Sense: 0.245 V @500 V (ratio 0.00049, 1:2000). R3003 being changed
     50k -> 5k in the next schematic rev (drawn 50k = 2.49 V, x10). FW keeps
     the 0.245 V scaling.
   - Insulation dead-short reads ~0.50 V (not 0.049 V); 0.045 V = 10 Mohm go/no-go
     unchanged/correct. Interpretation tables re-derived over 0..0.50 V.
   - Insulation return RESOLVED: energise HS[n], close all LS relays EXCEPT net n's
     own return -> net measured vs all other conductors on HV_RET (Sec 4.0/4.1/4.2).
   - HV card = 64 HS + 64 LS PER card; 256 lines needs 4 HV cards on shared I2C
     addr 0x20-0x27, bank-selected by ISO_HV_CARD_EN1..4 (confirmed by HW owner).
   - LO_COM pull-down = 100R (HW decision): sets the current-source return AND the
     continuity low leg. Resistance still needs low mux Ron / low current for
     COMPLIANCE (100R alone doesn't fix the 3.3 V clamp); planned reference self-cal
     removes the fixed offset. Continuity connected level is well below 1.5 V.


================================================================================
 MATRIX CARD REV "2" (2026-07-30) - WHAT CHANGED
================================================================================
Supersedes Matrix_Card-6. Task tracking lives in PROJECT_LOG.md; this section is
the firmware-facing summary.

   MUX SWAP: CD4067B (16:1) -> CD74HC4051 (8:1), 128 parts. HC family is
   characterised at 3.3 V, so the force loop drops from ~2 kohm to a few hundred
   ohm and the excitation ceiling rises from ~0.8 mA to several mA.
   Consequences for firmware:
     - 32 muxes per bank (was 16); enables HI_EN1..32 / LO_EN1..32
     - 3 channel-select bits S0..S2 (was 4); mux=(p-1)>>3, channel=(p-1)&0x07
     - enable bit map is BYTE-SWAPPED: GPB0..7 = EN1..8, GPA0..7 = EN9..16,
       so in the mcp23017 driver's packing: word = (val>>8)|(val<<8)

   TWO BUFFERED I2C SEGMENTS: 9 expanders will not fit in the MCP23017's 8
   addresses, so the bus is split by NTS0102DP level shifters:
     U141 OE = HI_S3 -> BUFF1 : U101 0x20 HI_EN1-16,  U102 0x21 LO_EN1-16,
                                U105 0x22 HI_EN17-32, U106 0x23 LO_EN17-32
     U142 OE = LO_S3 -> BUFF2 : U66  0x20 HI_SENSE_EN1-16,  U69 0x21 ADC ctl,
                                U67  0x22 HI_SENSE_EN17-32, U107 0x24
                                LO_SENSE_EN1-16, U108 0x26 LO_SENSE_EN17-32
   CORRECTED (HW-12, CL-22, 2026-08-11): the addresses above are read directly
   off Matrix_Card-7.pdf sheet 9, not the originally-assumed sequential
   0x24-0x27 block for the sense expanders - see the (now-resolved) BLOCKING
   item below for why U69 sits where it does.
   The select bit freed by 16:1 -> 8:1 became the segment select. Firmware must
   enable exactly one segment before any expander access.

   RESOLVED IN THIS REV: sense-enable net names (HI_SENSE_EN/LO_SENSE_EN now
   driven); one clock net (SPI1_SCLK everywhere incl. J101); ADC anti-alias front
   end fitted (R234/R235 4.99k 0.1% matched, C33 47nF differential, C142/C143
   4.7nF common-mode, ~320 Hz differential corner); 100k pull-up per mux retained
   on all 128; R131 still 100R 0.01%.

================================================================================
 OPEN ITEMS / ASSUMPTIONS TO CONFIRM
================================================================================
 BLOCKING
   - RESOLVED (HW-12, CL-22, 2026-08-11): the I2C-address-collision concern
     that used to be here (U101 vs U69 both assumed at 0x20) was based on an
     incorrect sequential-block assumption for the sense-side straps, not on
     what the schematic actually shows. Reading the real labels off
     Matrix_Card-7.pdf sheet 9 found U69 already sits on BUFF2 @ 0x21, between
     U66 (0x20) and U67 (0x22) - nowhere near U101 (0x20 on BUFF1, a different
     segment). No collision exists; no schematic change was needed.
     matrix_card.h's strap constants were wrong to match the old assumption
     and have been corrected to match the real addresses.

 MATRIX REV 2 FOLLOW-UPS
   - AD7476 (U33) removed from the Matrix card. Continuity still works via
     HI_COM -> J101 -> IN -> Control SPDT -> ADC_IN. BUT in resistance mode the
     SPDT selects I_OUT and disconnects ADC_IN, so NOTHING can observe HI_COM
     during current injection: the loopback compliance sweep cannot be done
     directly. Workaround in FW: sweep current on a 0R loopback and watch for
     R = V/I departing from linear. Proper fix: route HI_COM -> AIN2 and
     LO_COM -> AIN3 (both free) - also closes the current-reference item below.
   - HW-04 still NOT implemented: LO_COM is not routed to a spare ADC input, so
     the 0.01% R131 cannot serve as a current reference and resistance accuracy
     rides on the DAC8775 alone.                        [HW - awaiting decision]
   - SPI1_CS is still on J101 but has no consumer now that the AD7476 is gone.
   - HI_S3 / LO_S3 no longer reach any mux - they are the BUFF1/BUFF2 OE lines.
     Confirm NTS0102DP OE polarity before writing them.

 FIRMWARE REWORK - DONE (was "queued" against the rev-6 geometry; all landed)
   - matrix_card: DONE (FW-03, 2026-08-01) - 32 muxes/bank, 3 select bits, 4
     force + 4 sense expanders, segment switching via HI_S3/LO_S3 in the U21
     word, byte-swapped enable map, AD7476 binding removed.
   - continuity: measurement is on the Control front-end ADC (unchanged from
     the original design, never needed to move); thresholds still carry the
     1.3-1.7 V band from the 100k-return era and want a bring-up recheck.
   - kelvin: DONE (FW-02, CL-23, 2026-08-11) - rewritten for 4-wire, PGA
     auto-range, zero-current baseline subtraction. See the PENDING-FIRMWARE
     resistance-test entry above for detail.
   - drivers/ads124s08: DONE (FW-01, CL-10, 2026-08-01). CS/RESET/START/DRDY
     are on an I2C expander (U69), not GPIO, so the driver takes an io vtable
     rather than port/pin - and board.c now actually instantiates it (that
     wiring was still missing as of FW-01 and was added alongside FW-02).
   - dac8775 current-source register map      [still VERIFY vs datasheet]

 CARRIED OVER
   - Pin->(mux,channel) linear mapping (matrix + HV)      [assumed; need netlist]
   - HV DAC8830 CS pin (CS_ISO) + SPI2 mixed data-size    [VERIFY from netlist]
   - CA05P-5 output capacitance (sets discharge wait)     [TBD]
   - Number of HV boards in initial build (1..4)          [TBD]
   - 4 card slots on the Control card vs 4 HV cards + 1 Matrix card = 5 [TBD]
   - HV_Sense divider: R3003 50k -> 5k [DONE, HW-08, confirmed on HV_Card-3.pdf]

================================================================================
 BENCH SETUP - ADS1232 4-WIRE RIG (2026-07-30)
================================================================================
Was a stand-in for the ADS124S08 while the Matrix Card's Kelvin hardware did
not exist yet; that hardware is now real and the product build reads the
ADS124S08 directly (FW-02). This bench rig proved the 4-wire method (0.033R
resistor to 0.4%, no current calibration - Doc/4wire_resistance_validation.md)
and is now kept only for bench-side sanity checks, not as the primary path.
Built as a separate, macro-gated driver so it never ships in the product build.

   Enable with:  -DHT_ENABLE_ADS1232=1     (default 0 = not compiled)
   Files:        Core/Inc/drivers/ads1232.h, Core/Src/drivers/ads1232.c
   Docs:         Doc/ADS1232_bench_wiring.md

   KEY POINT: the ADS1232 is NOT an SPI device. There is no chip select and no
   MOSI. It has one clock in (SCLK) and one data line out (DOUT/DRDY) that
   doubles as the data-ready flag. It is driven bit-banged on two GPIOs; no SPI
   peripheral is used. Gain, data rate and channel are set by STATIC PINS, not
   registers - either strap them with jumpers or drive them from GPIO.

   TODO before this rig is trusted:
   - Add Datasheet/ads1232.pdf. Pin NUMBERS and the offset-calibration extra-
     SCLK-pulse sequence are marked VERIFY in the driver until then.
   - ADS1232 has NO internal reference: REFP/REFN must be supplied externally.
   - Confirm measured excitation current with a DMM before trusting any ohms.
================================================================================
